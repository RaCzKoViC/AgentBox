# Release Notes — AgentBox v5.0.0 Stable

Production hardening: backup/restore, integrity, crash recovery, maintenance mode, retention, versioned migrations, lifecycle, doctor/diagnostics, security checks, upgrade/rollback stubs.

Keeps rc1 Control Plane (REST/WS/Dashboard), beta3 Policy/Budget/Risk, Codex runner.

See `/workspace/agentbox-runs/live/stable-report.md` for deployment verification on `cursor`.
