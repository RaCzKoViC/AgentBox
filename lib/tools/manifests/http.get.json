{
  "id": "http.get",
  "name": "http.get",
  "display_name": "HTTP GET",
  "category": "http",
  "provider": "builtin",
  "version": "1.0.0",
  "capabilities": ["http_get", "http.get"],
  "permissions": ["network"],
  "risk": {"default": "MODERATE"},
  "execution": {"mode": "local", "timeout": 30},
  "input_schema": {
    "type": "object",
    "required": ["url"],
    "properties": {
      "url": {"type": "string"},
      "timeout": {"type": "integer", "default": 15},
      "headers": {"type": "object"}
    }
  },
  "output_schema": {
    "type": "object",
    "properties": {
      "ok": {"type": "boolean"},
      "status_code": {"type": "integer"},
      "body": {"type": "string"},
      "headers": {"type": "object"}
    }
  }
}
