# Sandbox (alpha3)

Modes: `worktree` | `docker` | `direct`

- worktree: `git worktree add` when project is a git repo
- docker: skipped with WARN if daemon unavailable
- direct: use project path as-is

Path: `~/.local/share/agentbox/v5/data/tasks/<task_id>/worktree/`
